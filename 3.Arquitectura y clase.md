# Arquitectura del Sistema y Referencias de la clase

Para garantizar la modularidad y el correcto procesamiento de los datos, el proyecto se diseñó bajo una **Arquitectura Orientada a Objetos (OO)** estructurada en 4 capas dinámicas. Esto permite aislar la interfaz gráfica de la complejidad de las librerías de bajo nivel del fabricante.

![Arquitectura del Sistema](arquitectura.jpeg)

### Descripción de las Capas:

1. **Capa de Aplicación (Usuario / GUI / Simulink):** Interfaces gráficas (MATLAB App) y modelos de simulación que interactúan directamente con el operador de forma intuitiva.
2. **Capa de Software (Clases MATLAB):** Núcleo lógico del proyecto que abstrae las funciones del SDK en componentes especializados:
   * **`LV9063`:** Clase principal encargada de la conexión y gestión del hardware.
   * **`AnalogOutput` & `Acquisition`:** Controladores de configuración de señales de entrada (lecturas, triggers, muestreo) y salida (amplitud, frecuencia).
   * **`Processing`:** Módulo matemático que calcula en tiempo real variables críticas como valores RMS, potencias (P, Q, S), armónicos e impedancias.
3. **Capa SDK (.NET):** El puente de comunicación que traduce las órdenes de MATLAB hacia el driver mediante las bibliotecas `LV9063SDK.dll` y `WinusbWrapper.dll`.
4. **Capa Hardware (LV9063):** El módulo físico de Festo Labvolt (entradas analógicas, relés, salidas digitales, encoder y puerto USB).


# LV9063EntryPoint

![Clases de matlab](aclasesmatlab.png)


La clase **LV9063EntryPoint** pertenece al SDK proporcionado por el fabricante **Festo/LabVolt** (licencia **9069-90**) y es la encargada de establecer la comunicación entre el software y el dispositivo de adquisición de datos **LV9063 (DAQ)**.

En otras palabras, esta clase actúa como un intermediario entre el hardware y la aplicación desarrollada en MATLAB.

La comunicación puede representarse de la siguiente manera:

```text
SDK (.NET)
│
├── LV9063SDK.dll
└── WinusbWrapper.dll
        │
        ▼
LV9063EntryPoint
        │
        ▼
Aplicación desarrollada en MATLAB
(LV9063, DeviceInfo, AnalogOutput,
Acquisition, DigitalIO y Processing)
```

Desde MATLAB, esta clase se utiliza mediante la interoperabilidad con **.NET**, agregando previamente el ensamblado mediante `NET.addAssembly`. Una vez cargado, es posible crear una instancia de la clase y acceder a sus métodos como si fueran objetos propios de MATLAB.

Por esta razón, las clases desarrolladas en MATLAB (**LV9063**, **DeviceInfo**, **AnalogOutput**, **Acquisition**, **DigitalIO** y **Processing**) no interactúan directamente con el hardware. En realidad, funcionan como **wrappers** (envoltorios) que organizan y simplifican el acceso a los métodos de **LV9063EntryPoint**, proporcionando una interfaz más clara y fácil de utilizar.

---

## 1. Agrupación Funcional de los Métodos

### A. Gestión del Dispositivo (`LV9063`)
| Método | Argumentos | Retorno | Descripción |
| :--- | :--- | :--- | :--- |
| `LV9063EntryPoint` | — | Objeto `LV9063EntryPoint` | Constructor de la clase. Instancia inicial requerida. |
| `InitDevice` | `this` | `ErrorCode` | Inicializa y abre la comunicación USB con el hardware. |
| `CloseDevice` | `this` | `ErrorCode` | Cierra la comunicación USB y libera recursos. |
| `SetDB9` | `this`, `uint8` | `ErrorCode` | Configura el modo/estado del conector DB9 auxiliar. |
| `SetSync` | `this`, `logical` | `ErrorCode` | Activa/desactiva la sincronización con señales externas. |
| `GetNbDevicesConnected` *(Static)* | — | `int32` | Retorna la cantidad de unidades LV9063 conectadas vía USB. |
| `Equals` | `this`, `obj` | `logical` | Heredado de `System.Object`. Compara referencias .NET. |
| `GetHashCode` | `this` | `int32` | Identificador interno del objeto .NET. |
| `GetType` | `this` | `System.Type` | Retorna el tipo de objeto en tiempo de ejecución. |
| `ToString` | `this` | `System.String` | Representación en texto del objeto .NET. |

---

### B. Información del Dispositivo (`DeviceInfo`)
| Método | Argumentos | Retorno | Descripción |
| :--- | :--- | :--- | :--- |
| `GetFirmwareVersion` | `this`, `int32` *(salida)* | `[ErrorCode, int32]` | Obtiene la versión del firmware cargado en el equipo. |
| `GetSerialNumber` | `this` | `System.String` | Devuelve el número de serie único del hardware. |

---

### C. Adquisición de Datos (`Acquisition`)
| Método | Argumentos | Retorno | Descripción |
| :--- | :--- | :--- | :--- |
| `AcquireData` | `this`, `Single[]` | `ErrorCode` | Captura el bloque de datos analógicos configurado. |
| `SetAcqTable` | `this`, `single`, `Input[]`, `int32` | `ErrorCode` | Define frecuencia de muestreo, lista de canales y paquetes. Usado por `LV9063Backend` ($F_s = 20\,\text{kHz}$). |
| `SendAcqTable` | `this` | `ErrorCode` | Transfiere la tabla de adquisición al hardware. |
| `SetAllInputsRange` | `this`, `InputRange[]` | `ErrorCode` | Define los rangos (`Low`/`High`) de los 8 canales analógicos. |
| `SetInput` | `this`, `int32`, `Input` | `ErrorCode` | Asigna un canal físico a una posición de la tabla. |
| `SetInputRangeE` | `this`, `Input`, `InputRange` | `ErrorCode` | Ajusta el rango de un canal de tensión (E) individual. |
| `SetInputRangeI` | `this`, `Input`, `InputRange` | `ErrorCode` | Ajusta el rango de un canal de corriente (I) individual. |
| `SetNbInputs` | `this`, `int32` | `ErrorCode` | Define la cantidad de canales analógicos a adquirir. |
| `SetNbPackets` | `this`, `int32` | `ErrorCode` | Define el número de paquetes por adquisición. |
| `SetSamplingFreq` | `this`, `single` | `ErrorCode` | Define la frecuencia de muestreo de forma independiente. |
| `GetEncoderValue` | `this`, `int32` *(salida)* | `[ErrorCode, int32]` | Retorna la lectura actual del encoder incremental. |
| `SetEncoderPPR` | `this`, `Encoder` | `ErrorCode` | Configura los pulsos por revolución (PPR) del encoder. |
| `SetTrigSource` | `this`, `TrigSource` | `ErrorCode` | Define la fuente del disparo (trigger). |
| `SetTrigLevel` | `this`, `single` | `ErrorCode` | Define el nivel de tensión/corriente para el disparo. |
| `SetTrigSlope` | `this`, `TrigSlope` | `ErrorCode` | Define el flanco de disparo (ascendente/descendente). |
| `SetTrigDelay` | `this`, `int32` | `ErrorCode` | Establece el retardo posterior al disparo. |
| `SetTrigHardware` | `this`, `logical` | `ErrorCode` | Habilita o deshabilita el disparo por hardware. |
| `SetTrigTable` | `this`, `TrigSource`, `single`, `int32`, `TrigSlope` | `ErrorCode` | Configuración integral de parámetros de disparo. |

---

### D. Salidas Analógicas (`AnalogOutput`)
| Método | Argumentos | Retorno | Descripción |
| :--- | :--- | :--- | :--- |
| `SendAnalogOutputsTable` | `this` | `ErrorCode` | Transfiere la configuración de AO1/AO2 al dispositivo. |
| `SetAnalogOutputValue` | `this`, `int32`, `int32` | `ErrorCode` | Establece un nivel de tensión continuo (DC). |
| `SetAnalogOutputAmplitude` | `this`, `int32`, `single` | `ErrorCode` | Ajusta la amplitud de la señal en la salida analógica. |
| `SetAnalogOutputFrequency` | `this`, `int32`, `single` | `ErrorCode` | Ajusta la frecuencia de la señal en la salida analógica. |
| `SetAnalogOutputWaveform` | `this`, `int32`, `Waveform` | `ErrorCode` | Selecciona la forma de onda (senoidal, triangular, etc.). |
| `SetAnalogOutputFunction` | `this`, `int32`, `FunctionType` | `ErrorCode` | Define el tipo de función matemática de salida. |
| `SetAnalogOutputStartStop` | `this`, `int32`, `int32` | `ErrorCode` | Habilita o deshabilita la generación en un canal AO. |
| `SetFunctionGenerator` | `this`, `Waveform`, `single`, `single` | `ErrorCode` | Configura forma de onda, amplitud y frecuencia en un único comando. |

---

### E. Salidas Digitales (`DigitalIO`)
| Método | Argumentos | Retorno | Descripción |
| :--- | :--- | :--- | :--- |
| `SendDigitalOutputsTable` | `this` | `ErrorCode` | Transfiere el estado de las salidas digitales. |
| `SetDigitalOutputState` | `this`, `int32`, `logical` | `ErrorCode` | Configura el estado binario de un canal DO individual. |
| `SetDigitalOutput` | `this`, `logical`, `logical` | `ErrorCode` | Configura simultáneamente el estado de DO0 y DO1. |

---

### F. Procesamiento de Señales (`Processing`)
Proporciona rutinas optimizadas en C#/.NET para el cálculo de variables eléctricas directas:
- **Tensión / Corriente:** `GetRMSValue`, `GetDCValue`, `GetCFValue`, `GetRValue`, `GetPhaseValue`, `GetHarmonicValue`.
- **Potencia y Red:** `GetPNZValue`, `GetPQSValue`, `GetPQSThreePhaseValue`, `GetPowerFactorValue`, `GetPowerFactorThreePhaseValue`, `GetRXZValue`.
- **Mecánica / Conversión:** `GetTorqueValue` (con sobrecargas), `GetPmValue`.

---

## 2. Métodos Heredados de la Clase Base `handle` (MATLAB)

Adicionalmente, los objetos instanciados desde MATLAB incluyen métodos nativos de la clase base `handle` (`addlistener`, `delete`, `eq`, `findobj`, `gt`, `isvalid`, `le`, `lt`, `ne`, `notify`). Estos métodos gestionan la vida del objeto, eventos y comparaciones lógicas dentro del entorno de MATLAB, sin interacción directa con los registros del hardware DAQ.
